<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <base href="/static/site/">
    <link rel="stylesheet" href="css/login.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>
    <div class="container">
        <div class="form-container" id="login-form">
            <h1>Login</h1>
            <form method="post">
                <%if(error) { %>
                    <label style="color: red;"><%=error%></label>
                <%}%>
                <label for="username">Email</label>
                <input type="email" id="username" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" required autofocus>
                <label class="control-label" for="password">Password</label>
                <input type="password" name="password" id="myInput" required>
                <input style="position: relative; top: -46px; right: -237px;" type="checkbox" onclick="myFunction()">
                <button style="margin-top: -30px;" type="submit">Login</button>
            </form>
            <p>Bạn chưa có tài khoản? <a href="/register" id="signup-link">Đăng kí ngay</a></p>
            <p>Bạn không nhớ mật khẩu? <a href="/forgotPassword" id="signup-link">Quên mật khẩu</a></p>
            <p><a href="/" id="signup-link">Quay lại trang chủ</a></p>
        </div>
    </div>

    <script>
        function myFunction() {
            var x = document.getElementById("myInput");
            if (x.type === "password") {
                x.type = "text";
            } else {
                x.type = "password";
            }
        }
    </script>
</body>
</html>